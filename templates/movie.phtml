<main class="container my-4">
  <div class="card shadow-lg border-0">
    <div class="row g-0">
      <div class="col-md-4">
        <img src="images/posters/<?= $movie->id_pelicula ?>.jpg"
             class="img-fluid rounded-start"
             alt="<?= htmlspecialchars($movie->titulo) ?>">
      </div>

      <div class="col-md-8">
        <div class="card-body">
          <h3 class="card-title"><?= htmlspecialchars($movie->titulo) ?></h3>
          <p class="card-text">
            <strong>Año:</strong> <?= htmlspecialchars($movie->anio) ?><br>
            <strong>Rating:</strong> ⭐ <?= htmlspecialchars($movie->rating) ?>/10<br>
            <strong>Género:</strong>
            <a href="filmGender/<?= $movie->id_genero ?>" class="badge bg-info text-dark">
              <?= htmlspecialchars($movie->genero) ?>
            </a>
          </p>

          <div class="mt-2">
            <a href="home" class="btn btn-sm btn-outline-secondary">Volver</a>

            <!-- 🔹 Solo si está logueado -->
            <?php if ($user): ?>
              <a href="editFilm/<?= $movie->id_pelicula ?>" class="btn btn-sm btn-outline-warning">Editar</a>
              <a href="deleteFilm/<?= $movie->id_pelicula ?>"
                 class="btn btn-sm btn-outline-danger"
                 onclick="return confirm('¿Seguro que querés eliminar esta película?');">Eliminar</a>
            <?php endif; ?>
          </div>
        </div>
      </div>
    </div>
  </div>
</main>
